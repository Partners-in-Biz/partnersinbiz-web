/**
 * Design Iteration ("Design this page") — public API.
 *
 * P1 live-browser design iteration in Messages: org-scoped variant decks with
 * per-variant Accept/Reject actions, and an apply record written only after
 * an explicit Accept (development branch, approved repo only).
 */

export * from './types'
export * from './store'
export * from './iteration-card'
