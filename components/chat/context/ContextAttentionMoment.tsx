'use client'
import type { ChatContextAction, ContextAttentionSummary } from '@/lib/chat-context/types'
export function ContextAttentionMoment({ attention, onAction, pendingActionId }: { attention: ContextAttentionSummary; onAction?: (action: ChatContextAction) => void; pendingActionId?: string }) {
  return <div className="rounded-lg border border-amber-400/25 bg-[color-mix(in_srgb,var(--st-warning)_14%,transparent)] p-3"><p className="text-xs font-medium text-[var(--color-pib-text)]">{attention.label}</p>{attention.detail && <p className="mt-1 text-[10px] text-[var(--color-pib-text-muted)]">{attention.detail}</p>}<div className="mt-2 flex flex-wrap gap-2">{attention.actions?.map((action) => action.href && !action.method ? <a key={action.id} href={action.href} className="inline-flex min-h-11 items-center rounded-sm px-2 text-xs text-primary outline-none focus-visible:ring-2 focus-visible:ring-primary/60 xl:min-h-0">{action.label}</a> : <button key={action.id} type="button" disabled={pendingActionId === action.id} onClick={() => onAction?.(action)} className="min-h-11 rounded-md bg-primary px-3 py-1.5 text-xs text-on-primary outline-none disabled:opacity-50 focus-visible:ring-2 focus-visible:ring-primary/60 xl:min-h-0">{action.label}</button>)}</div></div>
}
